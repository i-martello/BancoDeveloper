import {createSlice} from '@reduxjs/toolkit'

const initialState = {
  name: ""
}

const cryptoSlice = createSlice({
  name: "crypto",
  initialState,
  reducers:{
    userValidate: (state, action) => {
  
      
    }
  }
})